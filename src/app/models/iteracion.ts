export class Iteracion {
    iteracion: string;
    xi: string;
    random: string;

    constructor(iteracion: string, xi: string, random: string) {
        this.iteracion = iteracion;
        this.xi = xi;
        this.random = random;
    }
}